---
import Logo from "./logo.astro"
const links = ['d']
---

<nav class="flex items-center justify-between py-10 px-14">
    <figure class="relative flex items-center gap-4 text-4xl text-primary">
        <Logo/>
        <figcaption class="font-title">
            <a href="/" class="before:absolute before:inset-0">loak.studio</a>
        </figcaption>
    </figure>
    <div>
        {links.map(link=>
        
        <p>lien</p>
        )}
    </div>
</nav>
