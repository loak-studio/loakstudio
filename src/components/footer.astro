---

import {getData } from "../functions"
const pages = await getData('pages')
---
<footer class="flex flex-col items-center justify-between mt-36 md:flex-row px-14 bg-primary-darkest py-7">
    <address class="text-lwhite">
    </address>
    <nav class="flex flex-col gap-12 md:flex-row text-lwhite">
        {pages.data.map(navItem=>
            <a class="hover:underline" href={'/' + (navItem.attributes.slug != "homepage" ? navItem.attributes.slug : '')}>{navItem.attributes.title}</a>
        )}
    </nav>
    <div class="text-right ">
        <div>
            
        </div>
        <p class="text-lwhite">&copy; 2022 loak.studio - Tous droits réservés</p>
        <div class="text-lwhite">
            <a href="#">Mentions légales</a> & <a href="#">Politique de confidentialité</a>
        </div>
    </div>
</footer>